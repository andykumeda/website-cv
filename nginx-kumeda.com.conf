# Nginx configuration for kumeda.com (main domain - landing page)
server {
    server_name kumeda.com www.kumeda.com;
    
    root /var/www/cv;
    index index.html;
    
    # Handle SPA routing - all routes serve index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # Optional: Add caching for static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/kumeda.com/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/kumeda.com/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
}

# HTTP to HTTPS redirect
server {
    if ($host = www.kumeda.com) {
        return 301 https://$host$request_uri;
    } # managed by Certbot

    if ($host = kumeda.com) {
        return 301 https://$host$request_uri;
    } # managed by Certbot

    server_name kumeda.com www.kumeda.com;
    listen 80;
    return 404; # managed by Certbot
}
